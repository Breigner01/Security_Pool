int main(void)
{
    FILE *stream = fopen("tmp.txt", O_RDONLY);
    char *str;
    size_t n = 0;
    ssize_t nb = getline(&str, &n, stream);
    int i = 0;

    while (str[i]) {
        str[i] -= 1;
        str[i]
    }
}
